# Dynamic-Website-Builder
A dynamic website builder created using HTML, CSS, and Vanilla JavaScript that demonstrates how reusable cards can be generated dynamically using DOM manipulation. The project focuses on building structured UI elements through JavaScript instead of static HTML.
